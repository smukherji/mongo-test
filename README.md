# Demo Express Route with Azure Cosmos DB - MongoDB API
Install the following dependencies:
```
npm install express body-parser mongoose --save
npm install nodemon --save
npm install mongodb --save
```

To start the app:
```
npm start
```
